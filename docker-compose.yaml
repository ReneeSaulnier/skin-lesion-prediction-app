services:
  csharp-backend:
    build:
      context: ./api/  # Path to the app
    container_name: csharp-backend
    ports:
      - "8080:8080"
    networks:
      - app-network
    volumes:
      - image-storage:/shared/images
  ml:
    build:
      context: ./ml/  # Path to the app
    container_name: ml
    ports:
      - "8000:8000"
    networks:
      - app-network
    volumes:
      - image-storage:/shared/images
  client:
    build:
      context: ./client/  # Path to the app
    container_name: fontend
    ports:
      - "3000:3000"
    networks:
      - app-network
    volumes:
      - image-storage:/shared/images
      
networks:
  app-network:
    driver: bridge

volumes:
  image-storage: